apiVersion: v1
kind: Service
metadata:
  name: {{ include "Simple-ADOT-Helm-Chart.fullname" . }}
  labels:
    {{- include "Simple-ADOT-Helm-Chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Simple-ADOT-Helm-Chart.selectorLabels" . | nindent 4 }}
