apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Simple-ADOT-Helm-Chart.fullname" . }}-test-connection"
  labels:
    {{- include "Simple-ADOT-Helm-Chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Simple-ADOT-Helm-Chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
